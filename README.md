# Brady Bot

Fantasy football platform. One app, one nav bar, sections for Lineup Picker and
True Depth Chart.

Everything lives in [`app/`](app/) — see [`app/README.md`](app/README.md) for how
to run it, the section layout, and how to add a new section.

```bash
cd app && ./run.sh
```
